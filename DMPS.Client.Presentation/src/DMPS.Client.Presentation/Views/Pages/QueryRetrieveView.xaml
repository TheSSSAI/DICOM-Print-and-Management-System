<Page x:Class="DMPS.Client.Presentation.Views.Pages.QueryRetrieveView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DMPS.Client.Presentation.Views.Pages"
      xmlns:vm="clr-namespace:DMPS.Client.Presentation.ViewModels.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=vm:QueryRetrieveViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="QueryRetrieveView">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Query Remote PACS" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0 0 0 16"/>

        <!-- Query Form -->
        <materialDesign:Card Grid.Row="1" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Target PACS"
                          ItemsSource="{Binding ConfiguredPacs}"
                          SelectedItem="{Binding SelectedPacs}"
                          DisplayMemberPath="AeTitle"
                          MinWidth="150"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"/>

                <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Patient ID" Margin="16 0 0 0"
                         Text="{Binding Criteria.PatientId}"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>

                <DatePicker Grid.Column="2" materialDesign:HintAssist.Hint="Study Date" Margin="16 0 0 0"
                            SelectedDate="{Binding Criteria.StudyDate}"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>

                <TextBox Grid.Column="3" materialDesign:HintAssist.Hint="Modality" Margin="16 0 0 0"
                         Text="{Binding Criteria.Modality}"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                
                <Button Grid.Column="4" Margin="16 0 0 0"
                        Command="{Binding QueryCommand}"
                        IsEnabled="{Binding IsNotBusy}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        VerticalAlignment="Center" Content="Query"/>
            </Grid>
        </materialDesign:Card>

        <!-- Results Grid -->
        <DataGrid Grid.Row="2" Margin="0 16"
                  ItemsSource="{Binding QueryResults}"
                  SelectionMode="Extended"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <i:Interaction.Behaviors>
                <behaviors:DataGridSelectedItemsBehavior SelectedItems="{Binding SelectedStudies}"/>
            </i:Interaction.Behaviors>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Patient Name" Binding="{Binding PatientName}" Width="2*"/>
                <DataGridTextColumn Header="Patient ID" Binding="{Binding PatientId}" Width="1.5*"/>
                <DataGridTextColumn Header="Study Date" Binding="{Binding StudyDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="1.5*"/>
                <DataGridTextColumn Header="Study Description" Binding="{Binding StudyDescription}" Width="3*"/>
                <DataGridTextColumn Header="Modality" Binding="{Binding Modalities}" Width="*"/>
                <DataGridTextColumn Header="Series Count" Binding="{Binding SeriesCount}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status and Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="20" Height="20" Margin="0 0 8 0" 
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Retrieve Selected"
                    Command="{Binding RetrieveCommand}"
                    IsEnabled="{Binding CanRetrieve}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Grid>
</Page>