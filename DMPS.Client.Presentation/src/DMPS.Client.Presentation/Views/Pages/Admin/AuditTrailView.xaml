<UserControl x:Class="DMPS.Client.Presentation.Views.Pages.Admin.AuditTrailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DMPS.Client.Presentation.ViewModels.Pages.Admin"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:AuditTrailViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter Toolbar -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,8" Padding="8">
            <StackPanel Orientation="Horizontal">
                <DatePicker materialDesign:HintAssist.Hint="Start Date" SelectedDate="{Binding StartDateFilter}" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>
                <DatePicker materialDesign:HintAssist.Hint="End Date" SelectedDate="{Binding EndDateFilter}" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>
                <TextBox materialDesign:HintAssist.Hint="Filter by User" Text="{Binding UserFilter, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,16,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalAlignment="Center"/>
                <Button Content="Apply Filter" Command="{Binding FilterLogsCommand}" Style="{StaticResource MaterialDesignRaisedButton}" VerticalAlignment="Center"/>
                <Button Content="Export to CSV" Command="{Binding ExportCsvCommand}" Margin="16,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Audit Log List -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding AuditLogs}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding EventTimestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss.fff}'}" Width="1.5*"/>
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="1*"/>
                <DataGridTextColumn Header="Event Type" Binding="{Binding EventType}" Width="1.5*"/>
                <DataGridTextColumn Header="Entity" Binding="{Binding EntityName}" Width="1*"/>
                <DataGridTextColumn Header="Entity ID" Binding="{Binding EntityId}" Width="1*"/>
                <DataGridTextColumn Header="Details" Binding="{Binding DetailsJson}" Width="4*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1" Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>