<Page x:Class="DMPS.Client.Presentation.Views.Pages.Admin.AuditTrailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DMPS.Client.Presentation.Views.Pages.Admin"
      xmlns:vm="clr-namespace:DMPS.Client.Presentation.ViewModels.Pages.Admin"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=vm:AuditTrailViewModel}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuditTrailView">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header and Actions -->
        <Grid Grid.Row="0" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Audit Trail" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding ExportCsvCommand}"
                    ToolTip="Export current view to CSV">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileDownload" Margin="0 0 8 0"/>
                    <TextBlock Text="Export CSV"/>
                </StackPanel>
            </Button>
        </Grid>
        
        <!-- Filter Bar -->
        <materialDesign:Card Grid.Row="1" Padding="16">
            <StackPanel Orientation="Horizontal">
                <ComboBox materialDesign:HintAssist.Hint="Filter by User"
                          ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUserFilter}"
                          DisplayMemberPath="Username"
                          Width="200"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"/>

                <DatePicker materialDesign:HintAssist.Hint="Start Date"
                            SelectedDate="{Binding StartDateFilter}"
                            Width="150" Margin="16 0 0 0"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>

                <DatePicker materialDesign:HintAssist.Hint="End Date"
                            SelectedDate="{Binding EndDateFilter}"
                            Width="150" Margin="16 0 0 0"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"/>

                <Button Content="Filter"
                        Command="{Binding FilterCommand}"
                        Margin="16 0 0 0" VerticalAlignment="Center"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </materialDesign:Card>
        
        <!-- Audit Log Grid -->
        <DataGrid Grid.Row="2" Margin="0 16 0 0"
                  ItemsSource="{Binding AuditLogs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="1.5*"/>
                <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Event Type" Binding="{Binding EventType}" Width="1.5*"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="4*"/>
                <DataGridTextColumn Header="Correlation ID" Binding="{Binding CorrelationId}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Indicator -->
        <Grid Grid.Row="2" Background="#77FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="50" Height="50"/>
        </Grid>
    </Grid>
</Page>