<UserControl x:Class="DMPS.Client.Presentation.Views.Pages.PrintPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DMPS.Client.Presentation.ViewModels.Pages"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:PrintPreviewViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Settings Panel -->
        <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="16" VerticalAlignment="Stretch">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Print Settings" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>

                    <ComboBox materialDesign:HintAssist.Hint="Printer" 
                              ItemsSource="{Binding AvailablePrinters}"
                              SelectedItem="{Binding SelectedPrinter}"
                              Margin="0,0,0,16"/>

                    <ComboBox materialDesign:HintAssist.Hint="Page Layout"
                              ItemsSource="{Binding AvailableLayouts}"
                              SelectedItem="{Binding SelectedLayout}"
                              Margin="0,0,0,16"/>
                    
                    <ComboBox materialDesign:HintAssist.Hint="Orientation"
                              ItemsSource="{Binding AvailableOrientations}"
                              SelectedItem="{Binding SelectedOrientation}"
                              Margin="0,0,0,16"/>
                    
                    <TextBlock Text="Margins (mm)" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" Margin="0,8,0,4"/>
                    <Slider Minimum="5" Maximum="20" Value="{Binding PageMargin}" TickFrequency="1" IsSnapToTickEnabled="True"/>

                    <!-- Other settings like branding, overlays etc. would go here -->

                </StackPanel>
            </ScrollViewer>
        </materialDesign:Card>
        
        <!-- Preview and Actions -->
        <DockPanel Grid.Column="1">
            <!-- Action Toolbar -->
            <materialDesign:Card DockPanel.Dock="Top" Padding="8" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Print" Command="{Binding PrintCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                    <Button Content="Export to PDF" Command="{Binding ExportPdfCommand}" Margin="8,0,0,0"/>
                    <Button Content="Send Email" Command="{Binding EmailCommand}" Margin="8,0,0,0"/>
                </StackPanel>
            </materialDesign:Card>
            
            <!-- Print Preview Area -->
            <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" Padding="10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <!-- A simplified representation of the print preview canvas -->
                    <Border Background="White"
                            Width="595" Height="842"  
                            Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <TextBlock Text="Print Preview Area"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   Foreground="LightGray"/>
                    </Border>
                </ScrollViewer>
            </Border>
        </DockPanel>

    </Grid>
</UserControl>