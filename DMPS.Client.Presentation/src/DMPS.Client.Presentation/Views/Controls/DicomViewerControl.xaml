<UserControl x:Class="DMPS.Client.Presentation.Views.Controls.DicomViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DMPS.Client.Presentation.ViewModels.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:DicomViewerViewModel}"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Black"
             Focusable="True"
             ClipToBounds="True">
    
    <Grid>
        <!-- The Image control will host the D3DImage for DirectX rendering in code-behind -->
        <Image x:Name="RenderSurface"
               Stretch="Uniform"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>

        <!-- Overlay for displaying image information -->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10" IsHitTestVisible="False">
            <TextBlock Text="{Binding PatientName}" Foreground="White" />
            <TextBlock Text="{Binding StudyDescription}" Foreground="White" />
        </StackPanel>

        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="10" IsHitTestVisible="False">
            <TextBlock Text="{Binding WindowLevel, StringFormat='WW/WL: {0}'}" Foreground="White"/>
            <TextBlock Text="{Binding Zoom, StringFormat='Zoom: {0:P0}'}" Foreground="White"/>
        </StackPanel>

        <!-- Loading Indicator -->
        <Grid Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Style="{StaticResource MaterialDesignCircularProgressBar}" Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>