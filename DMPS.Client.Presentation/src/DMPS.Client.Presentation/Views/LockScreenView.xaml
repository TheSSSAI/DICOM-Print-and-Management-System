<UserControl x:Class="DMPS.Client.Presentation.Views.LockScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DMPS.Client.Presentation.ViewModels"
             xmlns:behaviors="clr-namespace:DMPS.Client.Presentation.Infrastructure.Behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:LockScreenViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#AA000000">
        <materialDesign:Card Width="350" Padding="24" UniformCornerRadius="8" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <materialDesign:PackIcon Kind="Lock" Width="48" Height="48" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Session Locked" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                           HorizontalAlignment="Center" 
                           Margin="0,16,0,8"/>
                <TextBlock Text="{Binding Username, StringFormat='Session for {0} is locked.'}"
                           HorizontalAlignment="Center"
                           Style="{StaticResource MaterialDesignBody1TextBlock}"
                           Margin="0,0,0,24"/>

                <PasswordBox x:Name="PasswordBox"
                             materialDesign:HintAssist.Hint="Password"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             behaviors:PasswordBoxBindingBehavior.IsPasswordBindingEnabled="True"
                             behaviors:PasswordBoxBindingBehavior.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <PasswordBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding UnlockCommand}" CommandParameter="{Binding ElementName=PasswordBox}"/>
                    </PasswordBox.InputBindings>
                </PasswordBox>
                
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                           Margin="0,8,0,0"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullOrEmptyStringToVisibilityConverter}, ConverterParameter=invert}"/>

                <ProgressBar IsIndeterminate="True" 
                             Visibility="{Binding IsUnlocking, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Margin="0,16,0,0"/>

                <Grid Margin="0,24,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Log Out"
                            Grid.Column="0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding LogoutCommand}"
                            IsEnabled="{Binding IsNotUnlocking}"/>

                    <Button Content="Unlock"
                            Grid.Column="1"
                            IsDefault="True"
                            Command="{Binding UnlockCommand}"
                            CommandParameter="{Binding ElementName=PasswordBox}"
                            IsEnabled="{Binding IsNotUnlocking}"
                            Margin="8,0,0,0"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>